// Halaman Dashboard Default (Root Dashboard)
export default function DashboardPage() {
  return (
    // Tampilan kosong (placeholder) karena navigasi utama ada di sidebar
    // Biasanya user akan langsung memilih menu dari sidebar
    <div className="h-full flex items-center justify-center text-gray-400">
      {/* Empty Content / Placeholder */}
    </div>
  );
}
